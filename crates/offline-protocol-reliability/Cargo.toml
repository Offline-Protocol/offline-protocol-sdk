[package]
name = "offline-protocol-reliability"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
# AGPL-3.0-only requires the license text to accompany the program, and a
# published crate ships on its own — cargo copies this into the .crate archive.
license-file = "../../LICENSE"
repository.workspace = true
rust-version.workspace = true
description = "Reliability layer with ACK, retry, and deduplication for the Offline Protocol SDK"

[lints]
workspace = true

[dependencies]
offline-protocol-core = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]

[lib]
